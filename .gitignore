# Secrets — never commit these
*.pem
*.key
.env
.netrc
.pypirc
secrets_backup_*.zip

# SSH material — never commit any of this
.ssh/
ssh/
id_*
id_*.pub
authorized_keys*
known_hosts*
*_rsa
*_rsa.pub
*_ed25519
*_ed25519.pub
*_ecdsa
*_ecdsa.pub
*_dsa
*_dsa.pub

# Per-machine secrets/overrides (sourced by .profile)
.profile.local
profile.local
*.local

# macOS
.DS_Store

# Backup files created by install.sh
*.backup_*
